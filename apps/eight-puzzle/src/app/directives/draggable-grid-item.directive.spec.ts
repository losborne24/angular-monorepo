import { DraggableGridItemDirective } from './draggable-grid-item.directive';

describe('DraggableGridItemDirective', () => {
  it('should create an instance', () => {
    const directive = new DraggableGridItemDirective();
    expect(directive).toBeTruthy();
  });
});
