import { DragAndDropDemoImagesDirective } from './drag-and-drop-demo-images.directive';

describe('DragAndDropDemoImagesDirective', () => {
  it('should create an instance', () => {
    const directive = new DragAndDropDemoImagesDirective();
    expect(directive).toBeTruthy();
  });
});
